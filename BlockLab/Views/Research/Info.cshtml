@model ResearchWebModel
@{
    ViewBag.Title = "Результаты одного исследования";
}

<div class="container">
    <div class="row">
        <div class="col-sm-3 mb-2">
            <partial name="Partial/_LeftSidebarPartial" />
        </div>
        <div class="col-sm-9 mb-2">
            <div class="border-1 bg-light shadow rounded-1 p-2">
                <h1>@ViewBag.Title</h1>

                <p>Результаты одного лабораторного исследования:</p>
                
                <div class="row">
                    <div class="col-md-6">
                        <div>
                            <span>Дата: <strong>@Model.DateTime.ToString("dd.MM.yyyy")</strong></span>
                            <span>Время: <strong>@Model.DateTime.ToString("HH:mm")</strong></span>
                        </div>
                        <div>
                            <span>Название: <strong>@Model.Name</strong></span>
                        </div>
                        <div class="mt-2">
                            <span>Вид исследования: <strong>@Model.TypeResearchName</strong></span>
                        </div>
                        <div>
                            <span>Объект исследования: <strong>@Model.ResearchObjectName</strong></span>
                        </div>
                        <div class="mt-2">
                            <span>Результаты исследования:</span>
                        </div>
                        <div>
                            <span>Значение: <strong>@Model.Value.ToString("F1")</strong></span>
                        </div>
                        <div>
                            <span>Текст: <strong>@Model.Text</strong></span>
                        </div>
                        <div class="mt-2">
                            <span>Нормальность:</span>
                            @if (@Model.IsNormal)
                            {
                                <strong class="text-success">Нормальный результат</strong>
                            }
                            else
                            {
                                <strong class="text-danger">Плохой результат</strong>
                            }
                        </div>
                        <div>
                            <span>Примечание: @Model.Note</span>
                        </div>
                    </div>
                </div>

                <hr />

                <div class="row">
                    <div class="col-md-6">
                        <div>
                            <span>Лаборант: </span>
                        </div>
                        <div>
                            <span>Фамилия, имя, отчество: <strong>@Model.AssistantSurName @Model.AssistantFirstName @Model.AssistantPatronymic</strong></span>
                        </div>
                        <div>
                            <span>Рабочая смена: <strong>@Model.WorkShiftName</strong></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
